/**
 * Global test setup file for Jest.
 *
 * This file is automatically executed before every test.
 * Use it to import testing utilities, polyfills, or configure test environment.
 *
 * In this project, we import `@testing-library/jest-dom` to enable
 * custom matchers like `toBeInTheDocument`, `toHaveTextContent`, etc.
 */

import '@testing-library/jest-dom';